import os
import requests
import sys
import time

# --- الإعدادات ---
GEMINI_KEY = os.environ.get("GEMINI_KEY")
TELEGRAM_TOKEN = os.environ.get("TELEGRAM_TOKEN")
CHAT_ID = os.environ.get("CHAT_ID")
URL = os.environ.get("CSV_URL")

def send_telegram(text):
    url = f"https://api.telegram.org/bot{TELEGRAM_TOKEN}/sendMessage"
    payload = {"chat_id": CHAT_ID, "text": text, "parse_mode": "Markdown"}
    try:
        requests.post(url, data=payload, timeout=10)
    except:
        pass

def run_saudi_analyzer():
    try:
        print("📥 جاري سحب بيانات السوق السعودي...")
        response = requests.get(URL, timeout=30)
        
        if response.status_code != 200:
            send_telegram(f"⚠️ خطأ في المصدر: {response.status_code}")
            return

        csv_data = response.text.strip()

        if not csv_data or len(csv_data) < 10: 
            send_telegram("🔔 **قناص السوق السعودي:**\nلا توجد أسهم تطابق الفلتر حالياً.")
            return

        print("🤖 جاري التحليل وصياغة التقرير للجمهور...")
        
        gemini_endpoint = f"https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent?key={GEMINI_KEY}"
        
        # --- التعديل: إجبار البوت على معرفة الاسم من الرمز ---
        prompt = f"""
        أنت خبير فني في سوق الأسهم السعودي (تداول). مهمتك صياغة تقرير لقناة تليجرام عامة.
        لديك البيانات الفنية التالية (CSV):
        ```csv
        {csv_data}
        ```

        التعليمات الصارمة:
        1. **التعرف على الشركات:** استخدم **الرمز الرقمي (Symbol/Code)** لمعرفة الاسم العربي الصحيح للشركة. (مثال: إذا وجدت 1120 اكتب "الراجحي"، إذا وجدت 2010 اكتب "سابك"). **لا تترجم الاسم الإنجليزي حرفياً ولا تخترع أسماء.**
        2. **الاختيار:** اختر أفضل 5 فرص فنية واعدة (إيجابية) بناءً على المؤشرات.
        3. **التحذير:** اختر سهماً واحداً مؤشراته متضخمة جداً (إن وجد) وضعه في الملاحظة.
        4. **التنسيق:** التزم بهذا القالب نصياً دون تغيير:

        تقرير "قناص السوق السعودي" يُحدد 3 أسهم ذات إمكانات ارتدادية وصاعدة مدعومة بتحليل عدة مؤشرات فنية.

        • سهم [الاسم العربي الصحيح] ([الرمز]) عند [السعر] ريال، [سبب فني موجز جداً]، الهدف [هدف قريب] والوقف [وقف].

        • سهم [الاسم العربي الصحيح] ([الرمز]) عند [السعر] ريال، [سبب فني موجز جداً]، الهدف [هدف قريب] والوقف [وقف].

        • سهم [الاسم العربي الصحيح] ([الرمز]) عند [السعر] ريال، [سبب فني موجز جداً]، الهدف [هدف قريب] والوقف [وقف].

        • سهم [الاسم العربي الصحيح] ([الرمز]) عند [السعر] ريال، [سبب فني موجز جداً]، الهدف [هدف قريب] والوقف [وقف].

        • سهم [الاسم العربي الصحيح] ([الرمز]) عند [السعر] ريال، [سبب فني موجز جداً]، الهدف [هدف قريب] والوقف [وقف].

        ملاحظة: تم استبعاد سهم [الاسم العربي] ([الرمز]) بسبب [السبب].

        تنبيه: قرارات الشراء والبيع مسؤولية المُستثمر.
        ✦
        ✦
        ✦
        """

        payload = {
            "contents": [{"parts": [{"text": prompt}]}]
        }
        
        headers_gemini = {'Content-Type': 'application/json'}
        g_res = requests.post(gemini_endpoint, json=payload, headers=headers_gemini, timeout=30)
        
        if g_res.status_code == 429:
            time.sleep(20)
            g_res = requests.post(gemini_endpoint, json=payload, headers=headers_gemini, timeout=30)

        if g_res.status_code != 200:
            sys.exit(1)

        analysis = g_res.json()['candidates'][0]['content']['parts'][0]['text']
        send_telegram(analysis)
        print("✅ تم إرسال التقرير بالأسماء العربية الصحيحة!")

    except Exception as e:
        print(f"💥 Error: {str(e)}")
        sys.exit(1)

if __name__ == "__main__":
    run_saudi_analyzer()
